import React from 'react';
import {Text, View} from 'react-native';

const SearchBar = () => {
    return (
        <View>
            <Text>
                SearchBar
            </Text>
        </View>
    );
};

export default SearchBar;
