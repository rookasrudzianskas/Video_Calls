import React from 'react';
import {Text, View, StyleSheet} from 'react-native';

const Chat = () => {
    return (
        <View>

        </View>
    );
};

export default Chat;

const styles = StyleSheet.create({

});
